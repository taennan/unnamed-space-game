//
//  Helpers.swift
//  Unnamed Space Game
//
//  Created by Taennan Rickman on 9/12/20.
//

import SpriteKit

// Stores font names
struct Fonts {
    // Stops any objects being initialised with this type
    private init() {}
    
    static let ocr = "OCR A Std"
    
    /// Add more if needed (although I doubt that's necessary)
}


// Put this here just because
//  Prints empty lines in debugger
/// Uncomment if needed
/*
func makeSpace(_ lines: Int, preStatement: String = "", postStatement: String = "") {
    
    if preStatement != "" { print(preStatement) }
    
    for _ in 1...lines { print("") }
    
    if postStatement != "" { print(postStatement) }
    
}
*/
